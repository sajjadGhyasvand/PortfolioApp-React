import {AppBar, Button, Toolbar} from "@mui/material";

const Header = () => {
    return(
        <>
            <AppBar>
              <Toolbar>
                   <Button variant="secondery">کلیک کنید</Button>
               </Toolbar>
          </AppBar>
        </>
    )
}
export default Header;